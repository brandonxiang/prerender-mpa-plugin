export default interface optionsInterFace {
    indexPaths: Array<string>,
    renderOptions: any,
    staticDir: string,
    outputDir: string,
    routes: Array<string>,
    minify: object,
    server: object,
}